You have been spawned by OpenKanban for focused work on one ticket. Your CWD is a git worktree of the project repo — orient yourself there.

## First action

**Run `/prime` IMMEDIATELY before anything else.** It loads CLAUDE.md and the project memory at `~/.claude/projects/-Users-cmeid-manifold-dev/memory/`. You need this context.

## Your assignment

**Ticket Title:** {{.Title}}

**Ticket Description (pointer):**
{{.Description}}

**Git Branch:** {{.BranchName}} (from {{.BaseBranch}})

## Your brief

The ticket Description above is a one-line pointer, not the spec. The canonical brief lives in this repo at `tickets/<slug>.md` — derive `<slug>` from `{{.BranchName}}` by stripping the `task/` prefix (e.g., `task/tag-scenarios-by-pillar` → `tickets/tag-scenarios-by-pillar.md`).

Read it immediately after `/prime`. If it doesn't exist, STOP and ask Chris to author it before continuing — don't guess.

The brief uses sections `## Brief / ## Acceptance / ## Must NOT / ## File anchors / ## Context (read these)`. The Context section references memory files via `[[slug]]` tokens — for each token, read `~/.claude/projects/-Users-cmeid-manifold-dev/memory/<slug>.md`. If a referenced memory file doesn't exist, note it in your summary; don't fabricate.

## Ticket discipline

- **Scope:** work only inside this worktree. Don't modify other tickets' descriptions, status, or worktrees. If you need information from another ticket, read it — never edit.
- **WIP:** if you find yourself needing to context-switch across tickets or hold multiple unrelated concerns in mind, stop and recommend Chris split this ticket. Don't push through.
- **Status:** OpenKanban moved this ticket to `in_progress` on spawn. Don't change the status programmatically — Chris will move it to `done` after his review.

## Expectations

1. Run `/prime`.
2. Read `tickets/<slug>.md` and resolve its `[[slug]]` pointers.
3. Implement what the brief specifies.
4. Run validation appropriate to the change (tests, lint, vet).
5. Summarize the work for Chris.

Follow your global `~/.claude/CLAUDE.md` for everything else — push/PR, signing, ticket creation, etc. This template only defines OpenKanban-specific orchestration.

Begin by running `/prime`.
