# refactor openkanban daemon to optionally be started under control of the system

<!-- openkanban:card-notes start -->
## Notes (from openkanban card)

daemon should be independent of any tui, if desired by user. this allows much more rapid iteration on openkanban tui functions. user should select at install how to start the daemon by default, but have the ability to override in an individual session via a cli parameter
<!-- openkanban:card-notes end -->
